# Port-Scanner
A lightweight Python 3 port scanner that can scan a continuous port range or a custom list of ports (comma-separated or dash ranges). Easy to run from the command line — no external dependencies.
